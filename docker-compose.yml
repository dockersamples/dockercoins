version: "2"

services:
  rng:
    build: rng
    ports:
    - "8092:80"

  hasher:
    build: hasher
    ports:
    - "8092:80"

  webui:
    build: webui
    ports:
    - "8092:80"
    volumes:
    - "./webui/files/:/files/"

  redis:
    image: redis

  worker:
    build: worker

